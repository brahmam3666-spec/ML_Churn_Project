global:
  scrape_interval: 5s

rule_files:
  - "alert_rules.yml"

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - "localhost:9093"

scrape_configs:
  - job_name: 'ml-api'
    metrics_path: /metrics
    static_configs:
      - targets: ['127.0.0.1:5000']