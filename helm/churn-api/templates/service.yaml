apiVersion: v1
kind: Service
metadata:
  name: churn-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: churn-api
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}