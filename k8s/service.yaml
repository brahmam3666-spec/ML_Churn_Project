apiVersion: v1
kind: Service
metadata:
  name: churn-service
spec:
  type: NodePort
  selector:
    app: churn-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30007